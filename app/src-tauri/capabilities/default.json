{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs plus the native file-open dialog the import and setup screens feature-detect.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
